Shorthand: for numbers, re-assigns the variable
	=: assignment
		int a = 5;
			a = 10;
			sout(a); // 10


		+=: addition assignment (can be applicable to String)

	or logic: either, if either one is true, will get true
		only one condition has to be true
		False: if both conditions are false

	and logic: both, if both conditions are true, will return true
		true: if both conditions are true
		false: if one condition is false